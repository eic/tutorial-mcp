{
  "//": "GitHub Copilot / VS Code MCP configuration.",
  "//1": "Save as .vscode/mcp.json in your workspace (or paste the 'servers' block",
  "//2": "into your user settings.json under an \"mcp\" key). Then open the Copilot",
  "//3": "Chat view, switch to Agent mode, and the 'uproot' tools appear.",
  "//4": "The payload is the SAME docker command every MCP client uses.",
  "servers": {
    "uproot": {
      "type": "stdio",
      "command": "docker",
      "args": ["run", "-i", "--rm",
               "-v", "${workspaceFolder}/files/data:/data",
               "ghcr.io/eic/uproot-mcp-server:latest"]
    },
    "xrootd": {
      "type": "stdio",
      "command": "docker",
      "args": ["run", "-i", "--rm",
               "-e", "XROOTD_SERVER",
               "-e", "XROOTD_BASE_DIR",
               "ghcr.io/eic/xrootd-mcp-server:latest"],
      "env": {
        "XROOTD_SERVER": "root://dtn-eic.jlab.org",
        "XROOTD_BASE_DIR": "/volatile/eic/EPIC"
      }
    }
  }
}
